import type { LocalCloud } from 'declarations'
import type { Next, Request, Response } from 'phoenix-js/http'
import { v4 } from 'uuid'

export const cehckAcces = (req: Request<LocalCloud.SessionData>, res: Response, next: Next) => {

}